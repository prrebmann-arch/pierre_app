# MANDATORY: read ARCHITECTURE.md first

Before touching any code in this repo, read `ARCHITECTURE.md` at the root. It's a dense index of every route, API, component, table and pattern — designed to save 30-50k tokens vs scanning the repo. The "Ou chercher pour X" lookup table covers 30+ common tasks with exact paths.

If your change adds/removes a route, table, component family or context, **update ARCHITECTURE.md** in the same commit so the next session stays accurate.

@AGENTS.md
